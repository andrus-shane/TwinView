[Unit]
Description=ADB server for the console USB bridge
After=network.target

[Service]
ExecStart=/usr/bin/adb -a nodaemon server
Restart=always
RestartSec=2
User=root

[Install]
WantedBy=multi-user.target
